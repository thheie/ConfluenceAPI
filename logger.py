import logging

# FORMAT = '%(asctime)s %(clientip)-15s %(user)-8s %(message)s'
# logging.basicConfig(filename = 'update_filter_permissions.log', level = logging.INFO, format=FORMAT)
logger = logging.getLogger("permission_api")